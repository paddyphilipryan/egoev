<!--=================================
      Benefits -->
      <section class="space-ptb service-section bg-dark">
        <div class="container">
          <div class="row align-items-center">
            <div class="col-lg-5">
              <img class="img-fluid" src="{{ entry.sectionImage.one().getUrl('transform3') }}" alt="">
            </div>
            <div class="col-lg-7">
              <div class="service-description">
                <div class="section-title left-divider">
                  <span>{{ entry.commonExcerpt }}</span>
                  <h2 class="text-white">{{ entry.sectionTitle }}</h2>
                  <p class="text-white mb-0 ">{{ entry.serviceTextMain }}</p>
                </div>
                <div class="d-flex mt-4 mt-md-5">
                  <a href="#" class="btn btn-primary">Discover More</a>
                  <a href="#" class="btn btn-link text-white ms-4">Let's talk <i class="fa-solid fa-arrow-right-long ms-2"></i></a>
                </div>
              </div>
            </div>
          </div>
      </section>
      <!--=================================
      Benefits --> 
<!--=================================
      Benefits -->
      <section class="space-ptb service-section bg-dark">
        <div class="container">
          <div class="row align-items-center">
            <div class="col-lg-5">
              <img class="img-fluid" src="{{ entry.tab3Image.one().getUrl('transform3') }}" alt="">
            </div>
            <div class="col-lg-7">
              <div class="service-description">
                <div class="section-title left-divider">
                  <span>{{ entry.servicesSubtitle }}</span>
                  <h2 class="text-white">{{ entry.servicesTitle }}</h2>
                  <p class="text-white mb-0 ">{{ entry.whiteText }}</p>
                </div>
            
              </div>
            </div>
          </div>
      </section>
      <!--=================================
      Benefits --> 
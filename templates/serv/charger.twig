<!--=================================
      service -->
      <section class="space-pt">
        <div class="container">
          <div class="row align-items-center">
            <div class="col-lg-6">
              <div class="section-title">
                <h2>{{ entry.appMainHeading }}</h2>
                <p class="mb-0">{{ entry.appBodyText }}</p>
              </div>
            </div>
            <div class="col-lg-6 text-center">
              <img class="img-fluid" src="{{ entry.splitImage.one().getUrl('transform1') }}" alt="{{ entry.appMainHeading }}">
            </div>
          </div>
        </div>
      </section>
      <!--=================================
      service -->
<!--=================================
      service -->
      <section class="space-pt">
        <div class="container">
          <div class="row align-items-center">
            <div class="col-lg-6">
              <div class="section-title left-divider">
              <span>{{ entry.recentSubtitle }}</span>
                <h2>{{ entry.appMainHeading }}</h2>
                <p class="mb-0">{{ entry.appBodyText }}</p>
              </div>
            </div>
            <div class="col-lg-6 text-center">
              <img class="img-fluid" src="{{ entry.splitImage.one().getUrl('transform3') }}" alt="{{ entry.appMainHeading }}">
            </div>
          </div>
        </div>
      </section>
      <!--=================================
      service -->
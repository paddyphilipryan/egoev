 <!--=================================
      service -->
      <section class="space-ptb">
        <div class="container">
          <div class="row align-items-center">
            <div class="col-lg-5">
              <img class="img-fluid" src="{{ entry.splitImage.one().getUrl('transform1') }}" alt="{{ entry.title }}">
            </div>
            <div class="col-lg-7">
              <div class="section-title mt-3 mt-md-4 mt-lg-0 mb-0 ms-0 ms-lg-5">
                <h2>{{ entry.title }}</h2>
                <p class="mb-0">{{ entry.mainContent}}</p>
              </div>
            </div>
          </div>
        </div>
      </section>
      <!--=================================
